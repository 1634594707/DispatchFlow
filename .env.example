# DispatchFlow local overrides (copy to .env or use back/run-dev.ps1 env)
# Do NOT commit real API keys.

# --- Backend (optional; back/run-dev.ps1 sets defaults) ---
# FSD_API_BASE=http://localhost:8080
# FSD_AMAP_WEB_SERVICE_KEY=
# FSD_ADMIN_USER=admin
# FSD_ADMIN_PASSWORD=admin123

# --- Frontend: copy front/.env.example → front/.env.local for map keys ---
