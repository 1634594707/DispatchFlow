/**
 * 试点车队的对外规格文案（§4 T2-d，2026-09-25 按补能网络新拓扑重写）。
 *
 * <p>为什么是一段静态文案而不是库字段：`t_vehicle` 没有续航/容量列（`V01__init_schema.sql:61-83`），
 * 路线图 §7.3 明确"加列的迁移+seed+部署成本 > 展示收益"，所以这里只做展示口径，不参与任何判据。
 *
 * <p>数字口径只从路线图 §0.1 抄：**对外用 X3 满载续航 180 km**。标称最长续航那档（200 km）
 * 一律不对外引用（§0.1 的"对外口径=不引用"），所以这段文案里出现它属于回归，不是笔误该顺手改。
 *
 * <p>⚠ "约 2 h 充满"是**厂商标称的物理值**，不是仿真节拍：仿真里充满一台是
 * `fsd.fleet.energy.charge-rate-per-tick`（现役 4%/tick）≈ 12.5 s，因为演示要在几分钟内看到
 * 电量回来。两个数不是一回事，别拿仿真时间去质疑 2 h，也别拿 2 h 去校准仿真。
 *
 * <p>"35 台 / 母港 + 2 个补能点"对应活库设施规模（`zjf_standby_slots.sql` 35 个母港位、
 * `zjf_energy_sites.sql` 2 个卫星点 16 位、加基地 6 位共 22 个充电位）。
 * 旧文案里的"35 柜"已删：地图上那 35 个 `FSD-SWAP-*` 只是**柜的位置**，
 * 真正建模的换电柜是 `t_battery_swap_cabinet`（现役 2 台，其中卫星点 E1 那台 8 位）——
 * 拿"35 柜"当容量说，等于把一个还没落地的数字讲成事实。
 */
export const PILOT_VEHICLE_SPEC = '新石器 L4 · X3 满载续航 180 km · 约 2 h 充满 · 30 s 快速换电 · 35 台 · 母港 + 2 个补能点'
