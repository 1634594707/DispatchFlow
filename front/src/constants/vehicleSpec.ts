/**
 * 试点车队的对外规格文案（§4 T2-d）。
 *
 * <p>为什么是一段静态文案而不是库字段：`t_vehicle` 没有续航/容量列（`V01__init_schema.sql:61-83`），
 * 路线图 §7.3 明确"加列的迁移+seed+部署成本 > 展示收益"，所以这里只做展示口径，不参与任何判据。
 *
 * <p>数字口径只从路线图 §0.1 抄：**对外用 X3 满载续航 180 km**。标称最长续航那档（更高的值）
 * 一律不对外引用（§0.1 的"对外口径=不引用"），所以这段文案里出现它属于回归，不是笔误该顺手改。
 * `35 柜` 与活库 `t_station` 里 ACTIVE 的 `FSD-SWAP-*` 行数一致；柜数若变，改的是 §0.1 口径再同步这里。
 */
export const PILOT_VEHICLE_SPEC = '新石器 L4 · X3 满载续航 180 km · 30 s 快速换电 · 35 柜'
