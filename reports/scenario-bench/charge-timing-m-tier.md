# 补能时机对照（M 档 · 齐次到达）

档位 **M**；两臂同种子同重复次数（n=12），差值是**逐次配对相减**后再取均值与 95% 置信区间。

- A 臂：`chargeTiming=OPPORTUNISTIC`（空闲即补能，现状 `idleChargeWhenNoDemand=true`）
- B 臂：`chargeTiming=NEVER`（只在必充阈值以下才回桩）

| 指标 | A 均值 | B 均值 | 差 (A-B) | 95% CI | 判定 |
| --- | --- | --- | --- | --- | --- |
| completion_rate | 0.8941 | 0.9523 | -0.0582 | -0.0737 .. -0.0428 | A 低于 B |
| completion_mean_s | 750.7494 | 736.4636 | +14.2858 | +8.1025 .. +20.4690 | A 高于 B |
| pickup_wait_mean_s | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| avg_pickup_distance_m | 505.4727 | 437.2984 | +68.1743 | +40.9226 .. +95.4259 | A 高于 B |
| relocations | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completed_orders | 99.6667 | 106.4167 | -6.7500 | -8.6681 .. -4.8319 | A 低于 B |
| total_distance_m | 136913.5803 | 138852.0773 | -1938.4970 | -6125.5091 .. +2248.5152 | 分不出来 |
| deadhead_share | 0.3677 | 0.3343 | +0.0334 | +0.0198 .. +0.0469 | A 高于 B |
| charge_sessions | 11.7500 | 2.5000 | +9.2500 | +8.4777 .. +10.0223 | A 高于 B |
| charge_blocked_car_min | 380.8750 | 0.0000 | +380.8750 | +343.4789 .. +418.2711 | A 高于 B |
| charge_travel_m_session | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| pile_session_spread | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| regret_vs_hindsight | 0.3293 | 0.2608 | +0.0685 | +0.0398 .. +0.0972 | A 高于 B |

## 判语规则

1. 只有"95% CI 不跨过 0"才写方向；跨 0 的指标一律报"分不出来"，不允许拿点值大小当结论。
2. **两臂完成量不同时，绝对量不可直接比**：里程/充电次数随"多派成的单"一起涨，A 臂里程高不等于更费 —— 要看的是比率型指标（完成率、空驶率、regret）。
3. 两臂只差一个开关，其余参数逐字相同；换任何参数都要重跑，不能只改一臂。

## 假设声明

- 距离 = 直线欧氏距离 × detourFactor。**detourFactor 已从假设转实测**：对扩范围路网最大连通分量的 5486 个可通行点对跑最短路/直线距离，均值 1.481、中位 1.374、P90 1.946（`scripts/geo/osm_to_road_graph.py::detour_factor`，统计写在 seed 头部）。取均值 1.481，§1.1 原假设 1.3 相当于中位数
- 均速 avgSpeedKmh = 17.84，来自 seed 头部 speed_weighted_avg_kmh（按边长加权的限速均值）。裁掉提取框外的路之后从 16.42 升到 17.84 —— 框外多是低速 service 路。仍是单一常数，不分路段等级
- 场景范围 1613 × 500 m = **现役**派单围栏的外接框（§0.1 实测）。这不是 §1.12 的规划范围（近场 L1 2.19 km² + 三条干线走廊 ≈11.0 km²）：原述的"M 档目标范围 1.35 km²"已作废（实测并集只有 0.992 km²，§13.71），而 L1/干线的点位要等走廊 OSM 重提取 + 吸附后才能进库，**届时必须重跑本表**（§1.12 落地顺序第④步）
- 服务时长固定 serviceSeconds（取 §0.2 实测装货 210 s + 卸货 264 s 之和 474 s），不含排队与人工干预
- 能耗 busyDrainMetersPerPercent = **1,800 m/1% SOC**（= 真车新石器 L4 满载 ≈180 km ÷ 100%，本人于 2026-09-23 提供的规格，与 FleetEnergyProperties 默认值一致）。**旧默认 150 m/1%（满电 15 km）低 12 倍，是 §13.72 撤掉那批产能/桩数结论的根因**；180 km 本身是"满载"保守值（标称 200 km ⇒ 2,000 m/1%），没扣温度与载重折损，所以它是**规格折算不是标定**，扰动带见 energy-sensitivity 那张扫描表
- 充电时长 chargeSeconds = **7,200 s / 次**（真车一次约 2 h，本人提供）。字段语义是"补满一个 SOC 工作带（20→90，70 个点）"的时长，把整笔 2 h 摊给这 70 个点 —— 这是 §1.1-b 的保守折算口径，比"按 0→100 比例折算（5,040 s）"悲观 30%。真车充电曲线无实测数据（§0.2），**假设，非标定**；库里 ZJF-CHG-01 的 avg_service_seconds 仍是仿真时代的 1,800 s，两套口径并存的收口挂在 §10.2 第 3 问
- 充电线性（默认）：一次补能的时长 = chargeSeconds ×（chargeCompleteSoc 90 − 当前 SOC）/（90 − 20），充到 90% 即恢复派单（三个阈值与 FleetEnergyProperties 同值）。回桩位移**默认不计**（chargeLayout 为空 = 原地瞬间开充，历史基线口径）；给了布局就按路网单程计入空驶与耗电，车落点移到该桩位所在点
- 充电曲线形状是**情景参数不是拟合**（Config.chargeCurve）：拐点与倍率都没有真车数据支撑（§0.2：仓库里没有真车真充电记录），所以只用它做敏感性扫描并给扰动带，不得称"充电模型"。默认 LINEAR 与历史逐字相同
- 补能点位只有 5 个站址是实测坐标（t_station GCJ 换米）。**库里 6 根桩 CP1..CP6 全挂在 ZJF-CHG-01 上且六个车位坐标逐字相同**，所以"选近桩"在现役设施下无从谈起 —— spreadSixOverFiveStations 那一臂是 §1.8 的情景假设（把同样 6 根桩摊到 5 个站址），不是现状；桩位在各点间的 2/1/1/1/1 分配同样是假设
- 补能时机用三档策略枚举（Config.chargeTiming），语义逐条对齐生产：NEVER = 只在必充阈值以下回桩；OPPORTUNISTIC = 现状 idleChargeWhenNoDemand=true（本 tick 无落空单就顺势补、只抢空桩不排队）；DEFER_UNDER_PRESSURE = 错峰推迟（近 pressureWindowTicks 个 tick 累计落单数达到 pressureDeferOrders 时连顺势补也推迟，但必充档不推迟 —— 同 shouldDeferReturnToCharge 的安全兜底）。注意：真实链路的高峰信号是 t_energy_forecast.pressure_p95 而不是 peak mode（§7.2 实测 peak cron 为 NULL）
- 不做路口冲突/MAPF：本实验台比较的是选车策略的相对差异，不是通行能力
- 事后基线只是**下界**：对每一笔被接下的单，取全车队（含繁忙车、不看 SOC 与占用）最近车辆的直线接驾距离 + 同样的载货段。它比实际里程必然偏小，所以 regret 偏乐观，不能当"最优解"引用
- 车辆位置在派单瞬间即改为卸货点。候选车本来就只从空闲车里选，所以这不影响选车距离，只会让事后下界更乐观（已并入上一条）
- §5 第 3 条列的"SOC 抛锚次数"在本模型里**恒为 0，且是构造性的**：全链路 SOC 前置检查（沿用生产的 canCompleteTaskWithSoc 语义）不允许接一趟跑不完的单，所以"跑到一半没电"不可观测。可观测的替代信号是 LOW_SOC 落单次数与"补能被桩位挡住"的车·分钟，两者都已导出
- 所有数字都必须带档位标签（S/M/L）；跨档引用即为违规（§1.3）

---

# 错峰推迟 vs 现状（M 档，M4 要的那个数）

档位 **M**；两臂同种子同重复次数（n=12），差值是**逐次配对相减**后再取均值与 95% 置信区间。

- A 臂：`chargeTiming=DEFER_UNDER_PRESSURE`（近 20 tick 落单 ≥ 3 时推迟顺势补）
- B 臂：`chargeTiming=OPPORTUNISTIC`（现状）

| 指标 | A 均值 | B 均值 | 差 (A-B) | 95% CI | 判定 |
| --- | --- | --- | --- | --- | --- |
| completion_rate | 0.8941 | 0.8941 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completion_mean_s | 750.3516 | 750.7494 | -0.3978 | -1.2735 .. +0.4778 | 分不出来 |
| pickup_wait_mean_s | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| avg_pickup_distance_m | 503.0318 | 505.4727 | -2.4408 | -7.8131 .. +2.9315 | 分不出来 |
| relocations | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completed_orders | 99.6667 | 99.6667 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| total_distance_m | 136706.5658 | 136913.5803 | -207.0146 | -662.6536 .. +248.6245 | 分不出来 |
| deadhead_share | 0.3665 | 0.3677 | -0.0012 | -0.0039 .. +0.0015 | 分不出来 |
| charge_sessions | 11.8333 | 11.7500 | +0.0833 | -0.1001 .. +0.2668 | 分不出来 |
| charge_blocked_car_min | 370.0208 | 380.8750 | -10.8542 | -15.9351 .. -5.7732 | A 低于 B |
| charge_travel_m_session | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| pile_session_spread | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| regret_vs_hindsight | 0.3268 | 0.3293 | -0.0026 | -0.0082 .. +0.0031 | 分不出来 |

## 判语规则

1. 只有"95% CI 不跨过 0"才写方向；跨 0 的指标一律报"分不出来"，不允许拿点值大小当结论。
2. **两臂完成量不同时，绝对量不可直接比**：里程/充电次数随"多派成的单"一起涨，A 臂里程高不等于更费 —— 要看的是比率型指标（完成率、空驶率、regret）。
3. 两臂只差一个开关，其余参数逐字相同；换任何参数都要重跑，不能只改一臂。

## 假设声明

- 距离 = 直线欧氏距离 × detourFactor。**detourFactor 已从假设转实测**：对扩范围路网最大连通分量的 5486 个可通行点对跑最短路/直线距离，均值 1.481、中位 1.374、P90 1.946（`scripts/geo/osm_to_road_graph.py::detour_factor`，统计写在 seed 头部）。取均值 1.481，§1.1 原假设 1.3 相当于中位数
- 均速 avgSpeedKmh = 17.84，来自 seed 头部 speed_weighted_avg_kmh（按边长加权的限速均值）。裁掉提取框外的路之后从 16.42 升到 17.84 —— 框外多是低速 service 路。仍是单一常数，不分路段等级
- 场景范围 1613 × 500 m = **现役**派单围栏的外接框（§0.1 实测）。这不是 §1.12 的规划范围（近场 L1 2.19 km² + 三条干线走廊 ≈11.0 km²）：原述的"M 档目标范围 1.35 km²"已作废（实测并集只有 0.992 km²，§13.71），而 L1/干线的点位要等走廊 OSM 重提取 + 吸附后才能进库，**届时必须重跑本表**（§1.12 落地顺序第④步）
- 服务时长固定 serviceSeconds（取 §0.2 实测装货 210 s + 卸货 264 s 之和 474 s），不含排队与人工干预
- 能耗 busyDrainMetersPerPercent = **1,800 m/1% SOC**（= 真车新石器 L4 满载 ≈180 km ÷ 100%，本人于 2026-09-23 提供的规格，与 FleetEnergyProperties 默认值一致）。**旧默认 150 m/1%（满电 15 km）低 12 倍，是 §13.72 撤掉那批产能/桩数结论的根因**；180 km 本身是"满载"保守值（标称 200 km ⇒ 2,000 m/1%），没扣温度与载重折损，所以它是**规格折算不是标定**，扰动带见 energy-sensitivity 那张扫描表
- 充电时长 chargeSeconds = **7,200 s / 次**（真车一次约 2 h，本人提供）。字段语义是"补满一个 SOC 工作带（20→90，70 个点）"的时长，把整笔 2 h 摊给这 70 个点 —— 这是 §1.1-b 的保守折算口径，比"按 0→100 比例折算（5,040 s）"悲观 30%。真车充电曲线无实测数据（§0.2），**假设，非标定**；库里 ZJF-CHG-01 的 avg_service_seconds 仍是仿真时代的 1,800 s，两套口径并存的收口挂在 §10.2 第 3 问
- 充电线性（默认）：一次补能的时长 = chargeSeconds ×（chargeCompleteSoc 90 − 当前 SOC）/（90 − 20），充到 90% 即恢复派单（三个阈值与 FleetEnergyProperties 同值）。回桩位移**默认不计**（chargeLayout 为空 = 原地瞬间开充，历史基线口径）；给了布局就按路网单程计入空驶与耗电，车落点移到该桩位所在点
- 充电曲线形状是**情景参数不是拟合**（Config.chargeCurve）：拐点与倍率都没有真车数据支撑（§0.2：仓库里没有真车真充电记录），所以只用它做敏感性扫描并给扰动带，不得称"充电模型"。默认 LINEAR 与历史逐字相同
- 补能点位只有 5 个站址是实测坐标（t_station GCJ 换米）。**库里 6 根桩 CP1..CP6 全挂在 ZJF-CHG-01 上且六个车位坐标逐字相同**，所以"选近桩"在现役设施下无从谈起 —— spreadSixOverFiveStations 那一臂是 §1.8 的情景假设（把同样 6 根桩摊到 5 个站址），不是现状；桩位在各点间的 2/1/1/1/1 分配同样是假设
- 补能时机用三档策略枚举（Config.chargeTiming），语义逐条对齐生产：NEVER = 只在必充阈值以下回桩；OPPORTUNISTIC = 现状 idleChargeWhenNoDemand=true（本 tick 无落空单就顺势补、只抢空桩不排队）；DEFER_UNDER_PRESSURE = 错峰推迟（近 pressureWindowTicks 个 tick 累计落单数达到 pressureDeferOrders 时连顺势补也推迟，但必充档不推迟 —— 同 shouldDeferReturnToCharge 的安全兜底）。注意：真实链路的高峰信号是 t_energy_forecast.pressure_p95 而不是 peak mode（§7.2 实测 peak cron 为 NULL）
- 不做路口冲突/MAPF：本实验台比较的是选车策略的相对差异，不是通行能力
- 事后基线只是**下界**：对每一笔被接下的单，取全车队（含繁忙车、不看 SOC 与占用）最近车辆的直线接驾距离 + 同样的载货段。它比实际里程必然偏小，所以 regret 偏乐观，不能当"最优解"引用
- 车辆位置在派单瞬间即改为卸货点。候选车本来就只从空闲车里选，所以这不影响选车距离，只会让事后下界更乐观（已并入上一条）
- §5 第 3 条列的"SOC 抛锚次数"在本模型里**恒为 0，且是构造性的**：全链路 SOC 前置检查（沿用生产的 canCompleteTaskWithSoc 语义）不允许接一趟跑不完的单，所以"跑到一半没电"不可观测。可观测的替代信号是 LOW_SOC 落单次数与"补能被桩位挡住"的车·分钟，两者都已导出
- 所有数字都必须带档位标签（S/M/L）；跨档引用即为违规（§1.3）

---

# 真·错峰：高峰时段推迟返充 vs 现状（M 档 · **有峰段场景**，第 2 小时强度 ×2）

档位 **M**；两臂同种子同重复次数（n=12），差值是**逐次配对相减**后再取均值与 95% 置信区间。

- A 臂：`DEFER_IN_PEAK`（高峰段不顺势补，平段照常；必充档不受影响）
- B 臂：`OPPORTUNISTIC`（现状）

| 指标 | A 均值 | B 均值 | 差 (A-B) | 95% CI | 判定 |
| --- | --- | --- | --- | --- | --- |
| completion_rate | 0.6985 | 0.6475 | +0.0511 | +0.0460 .. +0.0561 | A 高于 B |
| completion_mean_s | 767.4668 | 773.2173 | -5.7505 | -9.5849 .. -1.9160 | A 低于 B |
| pickup_wait_mean_s | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| avg_pickup_distance_m | 602.1439 | 624.1282 | -21.9842 | -37.0767 .. -6.8918 | A 低于 B |
| relocations | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completed_orders | 121.1667 | 112.3333 | +8.8333 | +8.1790 .. +9.4877 | A 高于 B |
| total_distance_m | 176130.8002 | 166511.4986 | +9619.3016 | +7304.0873 .. +11934.5159 | A 高于 B |
| deadhead_share | 0.4132 | 0.4201 | -0.0069 | -0.0133 .. -0.0005 | A 低于 B |
| charge_sessions | 6.0000 | 12.5000 | -6.5000 | -7.2420 .. -5.7580 | A 低于 B |
| charge_blocked_car_min | 216.9792 | 254.4792 | -37.5000 | -44.9322 .. -30.0678 | A 低于 B |
| charge_travel_m_session | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| pile_session_spread | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| regret_vs_hindsight | 0.4334 | 0.4511 | -0.0177 | -0.0324 .. -0.0030 | A 低于 B |

## 判语规则

1. 只有"95% CI 不跨过 0"才写方向；跨 0 的指标一律报"分不出来"，不允许拿点值大小当结论。
2. **两臂完成量不同时，绝对量不可直接比**：里程/充电次数随"多派成的单"一起涨，A 臂里程高不等于更费 —— 要看的是比率型指标（完成率、空驶率、regret）。
3. 两臂只差一个开关，其余参数逐字相同；换任何参数都要重跑，不能只改一臂。

## 假设声明

- 距离 = 直线欧氏距离 × detourFactor。**detourFactor 已从假设转实测**：对扩范围路网最大连通分量的 5486 个可通行点对跑最短路/直线距离，均值 1.481、中位 1.374、P90 1.946（`scripts/geo/osm_to_road_graph.py::detour_factor`，统计写在 seed 头部）。取均值 1.481，§1.1 原假设 1.3 相当于中位数
- 均速 avgSpeedKmh = 17.84，来自 seed 头部 speed_weighted_avg_kmh（按边长加权的限速均值）。裁掉提取框外的路之后从 16.42 升到 17.84 —— 框外多是低速 service 路。仍是单一常数，不分路段等级
- 场景范围 1613 × 500 m = **现役**派单围栏的外接框（§0.1 实测）。这不是 §1.12 的规划范围（近场 L1 2.19 km² + 三条干线走廊 ≈11.0 km²）：原述的"M 档目标范围 1.35 km²"已作废（实测并集只有 0.992 km²，§13.71），而 L1/干线的点位要等走廊 OSM 重提取 + 吸附后才能进库，**届时必须重跑本表**（§1.12 落地顺序第④步）
- 服务时长固定 serviceSeconds（取 §0.2 实测装货 210 s + 卸货 264 s 之和 474 s），不含排队与人工干预
- 能耗 busyDrainMetersPerPercent = **1,800 m/1% SOC**（= 真车新石器 L4 满载 ≈180 km ÷ 100%，本人于 2026-09-23 提供的规格，与 FleetEnergyProperties 默认值一致）。**旧默认 150 m/1%（满电 15 km）低 12 倍，是 §13.72 撤掉那批产能/桩数结论的根因**；180 km 本身是"满载"保守值（标称 200 km ⇒ 2,000 m/1%），没扣温度与载重折损，所以它是**规格折算不是标定**，扰动带见 energy-sensitivity 那张扫描表
- 充电时长 chargeSeconds = **7,200 s / 次**（真车一次约 2 h，本人提供）。字段语义是"补满一个 SOC 工作带（20→90，70 个点）"的时长，把整笔 2 h 摊给这 70 个点 —— 这是 §1.1-b 的保守折算口径，比"按 0→100 比例折算（5,040 s）"悲观 30%。真车充电曲线无实测数据（§0.2），**假设，非标定**；库里 ZJF-CHG-01 的 avg_service_seconds 仍是仿真时代的 1,800 s，两套口径并存的收口挂在 §10.2 第 3 问
- 充电线性（默认）：一次补能的时长 = chargeSeconds ×（chargeCompleteSoc 90 − 当前 SOC）/（90 − 20），充到 90% 即恢复派单（三个阈值与 FleetEnergyProperties 同值）。回桩位移**默认不计**（chargeLayout 为空 = 原地瞬间开充，历史基线口径）；给了布局就按路网单程计入空驶与耗电，车落点移到该桩位所在点
- 充电曲线形状是**情景参数不是拟合**（Config.chargeCurve）：拐点与倍率都没有真车数据支撑（§0.2：仓库里没有真车真充电记录），所以只用它做敏感性扫描并给扰动带，不得称"充电模型"。默认 LINEAR 与历史逐字相同
- 补能点位只有 5 个站址是实测坐标（t_station GCJ 换米）。**库里 6 根桩 CP1..CP6 全挂在 ZJF-CHG-01 上且六个车位坐标逐字相同**，所以"选近桩"在现役设施下无从谈起 —— spreadSixOverFiveStations 那一臂是 §1.8 的情景假设（把同样 6 根桩摊到 5 个站址），不是现状；桩位在各点间的 2/1/1/1/1 分配同样是假设
- 补能时机用三档策略枚举（Config.chargeTiming），语义逐条对齐生产：NEVER = 只在必充阈值以下回桩；OPPORTUNISTIC = 现状 idleChargeWhenNoDemand=true（本 tick 无落空单就顺势补、只抢空桩不排队）；DEFER_UNDER_PRESSURE = 错峰推迟（近 pressureWindowTicks 个 tick 累计落单数达到 pressureDeferOrders 时连顺势补也推迟，但必充档不推迟 —— 同 shouldDeferReturnToCharge 的安全兜底）。注意：真实链路的高峰信号是 t_energy_forecast.pressure_p95 而不是 peak mode（§7.2 实测 peak cron 为 NULL）
- 不做路口冲突/MAPF：本实验台比较的是选车策略的相对差异，不是通行能力
- 事后基线只是**下界**：对每一笔被接下的单，取全车队（含繁忙车、不看 SOC 与占用）最近车辆的直线接驾距离 + 同样的载货段。它比实际里程必然偏小，所以 regret 偏乐观，不能当"最优解"引用
- 车辆位置在派单瞬间即改为卸货点。候选车本来就只从空闲车里选，所以这不影响选车距离，只会让事后下界更乐观（已并入上一条）
- §5 第 3 条列的"SOC 抛锚次数"在本模型里**恒为 0，且是构造性的**：全链路 SOC 前置检查（沿用生产的 canCompleteTaskWithSoc 语义）不允许接一趟跑不完的单，所以"跑到一半没电"不可观测。可观测的替代信号是 LOW_SOC 落单次数与"补能被桩位挡住"的车·分钟，两者都已导出
- 所有数字都必须带档位标签（S/M/L）；跨档引用即为违规（§1.3）

---

# 敏感性检查：把推迟条件调到最凶（M 档）

档位 **M**；两臂同种子同重复次数（n=12），差值是**逐次配对相减**后再取均值与 95% 置信区间。

- A 臂：窗口 = 整段仿真、阈值 = 1（本单之后只要落过一单就再也不顺势补）
- B 臂：`chargeTiming=OPPORTUNISTIC`（现状）

| 指标 | A 均值 | B 均值 | 差 (A-B) | 95% CI | 判定 |
| --- | --- | --- | --- | --- | --- |
| completion_rate | 0.9182 | 0.8941 | +0.0241 | +0.0102 .. +0.0380 | A 高于 B |
| completion_mean_s | 745.3287 | 750.7494 | -5.4207 | -8.9845 .. -1.8568 | A 低于 B |
| pickup_wait_mean_s | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| avg_pickup_distance_m | 478.2021 | 505.4727 | -27.2705 | -45.0776 .. -9.4635 | A 低于 B |
| relocations | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completed_orders | 102.4167 | 99.6667 | +2.7500 | +1.1443 .. +4.3557 | A 高于 B |
| total_distance_m | 137982.1360 | 136913.5803 | +1068.5557 | -1552.8290 .. +3689.9404 | 分不出来 |
| deadhead_share | 0.3548 | 0.3677 | -0.0129 | -0.0214 .. -0.0043 | A 低于 B |
| charge_sessions | 6.4167 | 11.7500 | -5.3333 | -6.5550 .. -4.1117 | A 低于 B |
| charge_blocked_car_min | 132.3333 | 380.8750 | -248.5417 | -309.4174 .. -187.6659 | A 低于 B |
| charge_travel_m_session | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| pile_session_spread | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| regret_vs_hindsight | 0.3028 | 0.3293 | -0.0266 | -0.0420 .. -0.0111 | A 低于 B |

## 判语规则

1. 只有"95% CI 不跨过 0"才写方向；跨 0 的指标一律报"分不出来"，不允许拿点值大小当结论。
2. **两臂完成量不同时，绝对量不可直接比**：里程/充电次数随"多派成的单"一起涨，A 臂里程高不等于更费 —— 要看的是比率型指标（完成率、空驶率、regret）。
3. 两臂只差一个开关，其余参数逐字相同；换任何参数都要重跑，不能只改一臂。

## 假设声明

- 距离 = 直线欧氏距离 × detourFactor。**detourFactor 已从假设转实测**：对扩范围路网最大连通分量的 5486 个可通行点对跑最短路/直线距离，均值 1.481、中位 1.374、P90 1.946（`scripts/geo/osm_to_road_graph.py::detour_factor`，统计写在 seed 头部）。取均值 1.481，§1.1 原假设 1.3 相当于中位数
- 均速 avgSpeedKmh = 17.84，来自 seed 头部 speed_weighted_avg_kmh（按边长加权的限速均值）。裁掉提取框外的路之后从 16.42 升到 17.84 —— 框外多是低速 service 路。仍是单一常数，不分路段等级
- 场景范围 1613 × 500 m = **现役**派单围栏的外接框（§0.1 实测）。这不是 §1.12 的规划范围（近场 L1 2.19 km² + 三条干线走廊 ≈11.0 km²）：原述的"M 档目标范围 1.35 km²"已作废（实测并集只有 0.992 km²，§13.71），而 L1/干线的点位要等走廊 OSM 重提取 + 吸附后才能进库，**届时必须重跑本表**（§1.12 落地顺序第④步）
- 服务时长固定 serviceSeconds（取 §0.2 实测装货 210 s + 卸货 264 s 之和 474 s），不含排队与人工干预
- 能耗 busyDrainMetersPerPercent = **1,800 m/1% SOC**（= 真车新石器 L4 满载 ≈180 km ÷ 100%，本人于 2026-09-23 提供的规格，与 FleetEnergyProperties 默认值一致）。**旧默认 150 m/1%（满电 15 km）低 12 倍，是 §13.72 撤掉那批产能/桩数结论的根因**；180 km 本身是"满载"保守值（标称 200 km ⇒ 2,000 m/1%），没扣温度与载重折损，所以它是**规格折算不是标定**，扰动带见 energy-sensitivity 那张扫描表
- 充电时长 chargeSeconds = **7,200 s / 次**（真车一次约 2 h，本人提供）。字段语义是"补满一个 SOC 工作带（20→90，70 个点）"的时长，把整笔 2 h 摊给这 70 个点 —— 这是 §1.1-b 的保守折算口径，比"按 0→100 比例折算（5,040 s）"悲观 30%。真车充电曲线无实测数据（§0.2），**假设，非标定**；库里 ZJF-CHG-01 的 avg_service_seconds 仍是仿真时代的 1,800 s，两套口径并存的收口挂在 §10.2 第 3 问
- 充电线性（默认）：一次补能的时长 = chargeSeconds ×（chargeCompleteSoc 90 − 当前 SOC）/（90 − 20），充到 90% 即恢复派单（三个阈值与 FleetEnergyProperties 同值）。回桩位移**默认不计**（chargeLayout 为空 = 原地瞬间开充，历史基线口径）；给了布局就按路网单程计入空驶与耗电，车落点移到该桩位所在点
- 充电曲线形状是**情景参数不是拟合**（Config.chargeCurve）：拐点与倍率都没有真车数据支撑（§0.2：仓库里没有真车真充电记录），所以只用它做敏感性扫描并给扰动带，不得称"充电模型"。默认 LINEAR 与历史逐字相同
- 补能点位只有 5 个站址是实测坐标（t_station GCJ 换米）。**库里 6 根桩 CP1..CP6 全挂在 ZJF-CHG-01 上且六个车位坐标逐字相同**，所以"选近桩"在现役设施下无从谈起 —— spreadSixOverFiveStations 那一臂是 §1.8 的情景假设（把同样 6 根桩摊到 5 个站址），不是现状；桩位在各点间的 2/1/1/1/1 分配同样是假设
- 补能时机用三档策略枚举（Config.chargeTiming），语义逐条对齐生产：NEVER = 只在必充阈值以下回桩；OPPORTUNISTIC = 现状 idleChargeWhenNoDemand=true（本 tick 无落空单就顺势补、只抢空桩不排队）；DEFER_UNDER_PRESSURE = 错峰推迟（近 pressureWindowTicks 个 tick 累计落单数达到 pressureDeferOrders 时连顺势补也推迟，但必充档不推迟 —— 同 shouldDeferReturnToCharge 的安全兜底）。注意：真实链路的高峰信号是 t_energy_forecast.pressure_p95 而不是 peak mode（§7.2 实测 peak cron 为 NULL）
- 不做路口冲突/MAPF：本实验台比较的是选车策略的相对差异，不是通行能力
- 事后基线只是**下界**：对每一笔被接下的单，取全车队（含繁忙车、不看 SOC 与占用）最近车辆的直线接驾距离 + 同样的载货段。它比实际里程必然偏小，所以 regret 偏乐观，不能当"最优解"引用
- 车辆位置在派单瞬间即改为卸货点。候选车本来就只从空闲车里选，所以这不影响选车距离，只会让事后下界更乐观（已并入上一条）
- §5 第 3 条列的"SOC 抛锚次数"在本模型里**恒为 0，且是构造性的**：全链路 SOC 前置检查（沿用生产的 canCompleteTaskWithSoc 语义）不允许接一趟跑不完的单，所以"跑到一半没电"不可观测。可观测的替代信号是 LOW_SOC 落单次数与"补能被桩位挡住"的车·分钟，两者都已导出
- 所有数字都必须带档位标签（S/M/L）；跨档引用即为违规（§1.3）

---

# ⑤ 同一个真·错峰对照，但**补能要开车过去**（现役布局：6 桩挂在 ZJF-CHG-01） —— §13.14③ 的修正数，替代那张表里的 completion_rate/接驾距离

档位 **M**；两臂同种子同重复次数（n=12），差值是**逐次配对相减**后再取均值与 95% 置信区间。

- A 臂：`DEFER_IN_PEAK` + 开去充电
- B 臂：`OPPORTUNISTIC` + 开去充电

| 指标 | A 均值 | B 均值 | 差 (A-B) | 95% CI | 判定 |
| --- | --- | --- | --- | --- | --- |
| completion_rate | 0.6931 | 0.6373 | +0.0558 | +0.0507 .. +0.0609 | A 高于 B |
| completion_mean_s | 770.4318 | 774.7858 | -4.3539 | -7.8414 .. -0.8665 | A 低于 B |
| pickup_wait_mean_s | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| avg_pickup_distance_m | 609.5290 | 624.4263 | -14.8973 | -28.9681 .. -0.8266 | A 低于 B |
| relocations | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completed_orders | 120.2500 | 110.5833 | +9.6667 | +8.9330 .. +10.4003 | A 高于 B |
| total_distance_m | 181351.2582 | 173706.0990 | +7645.1592 | +5587.4827 .. +9702.8357 | A 高于 B |
| deadhead_share | 0.4300 | 0.4484 | -0.0184 | -0.0236 .. -0.0131 | A 低于 B |
| charge_sessions | 6.0000 | 11.8333 | -5.8333 | -6.0807 .. -5.5860 | A 低于 B |
| charge_blocked_car_min | 216.9792 | 254.8958 | -37.9167 | -44.4951 .. -31.3383 | A 低于 B |
| charge_travel_m_session | 801.3224 | 756.7321 | +44.5903 | -21.4381 .. +110.6188 | 分不出来 |
| pile_session_spread | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| regret_vs_hindsight | 0.4451 | 0.4871 | -0.0421 | -0.0554 .. -0.0287 | A 低于 B |

## 判语规则

1. 只有"95% CI 不跨过 0"才写方向；跨 0 的指标一律报"分不出来"，不允许拿点值大小当结论。
2. **两臂完成量不同时，绝对量不可直接比**：里程/充电次数随"多派成的单"一起涨，A 臂里程高不等于更费 —— 要看的是比率型指标（完成率、空驶率、regret）。
3. 两臂只差一个开关，其余参数逐字相同；换任何参数都要重跑，不能只改一臂。

## 假设声明

- 距离 = 直线欧氏距离 × detourFactor。**detourFactor 已从假设转实测**：对扩范围路网最大连通分量的 5486 个可通行点对跑最短路/直线距离，均值 1.481、中位 1.374、P90 1.946（`scripts/geo/osm_to_road_graph.py::detour_factor`，统计写在 seed 头部）。取均值 1.481，§1.1 原假设 1.3 相当于中位数
- 均速 avgSpeedKmh = 17.84，来自 seed 头部 speed_weighted_avg_kmh（按边长加权的限速均值）。裁掉提取框外的路之后从 16.42 升到 17.84 —— 框外多是低速 service 路。仍是单一常数，不分路段等级
- 场景范围 1613 × 500 m = **现役**派单围栏的外接框（§0.1 实测）。这不是 §1.12 的规划范围（近场 L1 2.19 km² + 三条干线走廊 ≈11.0 km²）：原述的"M 档目标范围 1.35 km²"已作废（实测并集只有 0.992 km²，§13.71），而 L1/干线的点位要等走廊 OSM 重提取 + 吸附后才能进库，**届时必须重跑本表**（§1.12 落地顺序第④步）
- 服务时长固定 serviceSeconds（取 §0.2 实测装货 210 s + 卸货 264 s 之和 474 s），不含排队与人工干预
- 能耗 busyDrainMetersPerPercent = **1,800 m/1% SOC**（= 真车新石器 L4 满载 ≈180 km ÷ 100%，本人于 2026-09-23 提供的规格，与 FleetEnergyProperties 默认值一致）。**旧默认 150 m/1%（满电 15 km）低 12 倍，是 §13.72 撤掉那批产能/桩数结论的根因**；180 km 本身是"满载"保守值（标称 200 km ⇒ 2,000 m/1%），没扣温度与载重折损，所以它是**规格折算不是标定**，扰动带见 energy-sensitivity 那张扫描表
- 充电时长 chargeSeconds = **7,200 s / 次**（真车一次约 2 h，本人提供）。字段语义是"补满一个 SOC 工作带（20→90，70 个点）"的时长，把整笔 2 h 摊给这 70 个点 —— 这是 §1.1-b 的保守折算口径，比"按 0→100 比例折算（5,040 s）"悲观 30%。真车充电曲线无实测数据（§0.2），**假设，非标定**；库里 ZJF-CHG-01 的 avg_service_seconds 仍是仿真时代的 1,800 s，两套口径并存的收口挂在 §10.2 第 3 问
- 充电线性（默认）：一次补能的时长 = chargeSeconds ×（chargeCompleteSoc 90 − 当前 SOC）/（90 − 20），充到 90% 即恢复派单（三个阈值与 FleetEnergyProperties 同值）。回桩位移**默认不计**（chargeLayout 为空 = 原地瞬间开充，历史基线口径）；给了布局就按路网单程计入空驶与耗电，车落点移到该桩位所在点
- 充电曲线形状是**情景参数不是拟合**（Config.chargeCurve）：拐点与倍率都没有真车数据支撑（§0.2：仓库里没有真车真充电记录），所以只用它做敏感性扫描并给扰动带，不得称"充电模型"。默认 LINEAR 与历史逐字相同
- 补能点位只有 5 个站址是实测坐标（t_station GCJ 换米）。**库里 6 根桩 CP1..CP6 全挂在 ZJF-CHG-01 上且六个车位坐标逐字相同**，所以"选近桩"在现役设施下无从谈起 —— spreadSixOverFiveStations 那一臂是 §1.8 的情景假设（把同样 6 根桩摊到 5 个站址），不是现状；桩位在各点间的 2/1/1/1/1 分配同样是假设
- 补能时机用三档策略枚举（Config.chargeTiming），语义逐条对齐生产：NEVER = 只在必充阈值以下回桩；OPPORTUNISTIC = 现状 idleChargeWhenNoDemand=true（本 tick 无落空单就顺势补、只抢空桩不排队）；DEFER_UNDER_PRESSURE = 错峰推迟（近 pressureWindowTicks 个 tick 累计落单数达到 pressureDeferOrders 时连顺势补也推迟，但必充档不推迟 —— 同 shouldDeferReturnToCharge 的安全兜底）。注意：真实链路的高峰信号是 t_energy_forecast.pressure_p95 而不是 peak mode（§7.2 实测 peak cron 为 NULL）
- 不做路口冲突/MAPF：本实验台比较的是选车策略的相对差异，不是通行能力
- 事后基线只是**下界**：对每一笔被接下的单，取全车队（含繁忙车、不看 SOC 与占用）最近车辆的直线接驾距离 + 同样的载货段。它比实际里程必然偏小，所以 regret 偏乐观，不能当"最优解"引用
- 车辆位置在派单瞬间即改为卸货点。候选车本来就只从空闲车里选，所以这不影响选车距离，只会让事后下界更乐观（已并入上一条）
- §5 第 3 条列的"SOC 抛锚次数"在本模型里**恒为 0，且是构造性的**：全链路 SOC 前置检查（沿用生产的 canCompleteTaskWithSoc 语义）不允许接一趟跑不完的单，所以"跑到一半没电"不可观测。可观测的替代信号是 LOW_SOC 落单次数与"补能被桩位挡住"的车·分钟，两者都已导出
- 所有数字都必须带档位标签（S/M/L）；跨档引用即为违规（§1.3）

---

# ⑥ **8 小时班次**（8 h）里的「补不补」—— ①那张表是 2 小时窗口，而真车一次补能要 2 h，所以 ① 量到的是初始 SOC 的 transient，不是稳态

档位 **M**；两臂同种子同重复次数（n=12），差值是**逐次配对相减**后再取均值与 95% 置信区间。

- A 臂：`OPPORTUNISTIC`（空闲即补到 90%，现状语义）
- B 臂：`NEVER`（只在必充档以下回桩）

| 指标 | A 均值 | B 均值 | 差 (A-B) | 95% CI | 判定 |
| --- | --- | --- | --- | --- | --- |
| completion_rate | 0.9370 | 0.9273 | +0.0098 | -0.0068 .. +0.0263 | 分不出来 |
| completion_mean_s | 738.6691 | 746.7335 | -8.0644 | -12.6620 .. -3.4668 | A 低于 B |
| pickup_wait_mean_s | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| avg_pickup_distance_m | 435.2651 | 474.9138 | -39.6487 | -59.9601 .. -19.3373 | A 低于 B |
| relocations | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| completed_orders | 412.4167 | 408.0000 | +4.4167 | -2.6713 .. +11.5046 | 分不出来 |
| total_distance_m | 541079.3013 | 551719.7382 | -10640.4369 | -22431.3075 .. +1150.4337 | 分不出来 |
| deadhead_share | 0.3316 | 0.3510 | -0.0194 | -0.0290 .. -0.0098 | A 低于 B |
| charge_sessions | 252.9167 | 3.0000 | +249.9167 | +242.5444 .. +257.2889 | A 高于 B |
| charge_blocked_car_min | 621.8125 | 0.0000 | +621.8125 | +563.3679 .. +680.2571 | A 高于 B |
| charge_travel_m_session | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| pile_session_spread | 0.0000 | 0.0000 | +0.0000 | +0.0000 .. +0.0000 | 分不出来 |
| regret_vs_hindsight | 0.2598 | 0.2980 | -0.0382 | -0.0581 .. -0.0183 | A 低于 B |

## 判语规则

1. 只有"95% CI 不跨过 0"才写方向；跨 0 的指标一律报"分不出来"，不允许拿点值大小当结论。
2. **两臂完成量不同时，绝对量不可直接比**：里程/充电次数随"多派成的单"一起涨，A 臂里程高不等于更费 —— 要看的是比率型指标（完成率、空驶率、regret）。
3. 两臂只差一个开关，其余参数逐字相同；换任何参数都要重跑，不能只改一臂。

## 假设声明

- 距离 = 直线欧氏距离 × detourFactor。**detourFactor 已从假设转实测**：对扩范围路网最大连通分量的 5486 个可通行点对跑最短路/直线距离，均值 1.481、中位 1.374、P90 1.946（`scripts/geo/osm_to_road_graph.py::detour_factor`，统计写在 seed 头部）。取均值 1.481，§1.1 原假设 1.3 相当于中位数
- 均速 avgSpeedKmh = 17.84，来自 seed 头部 speed_weighted_avg_kmh（按边长加权的限速均值）。裁掉提取框外的路之后从 16.42 升到 17.84 —— 框外多是低速 service 路。仍是单一常数，不分路段等级
- 场景范围 1613 × 500 m = **现役**派单围栏的外接框（§0.1 实测）。这不是 §1.12 的规划范围（近场 L1 2.19 km² + 三条干线走廊 ≈11.0 km²）：原述的"M 档目标范围 1.35 km²"已作废（实测并集只有 0.992 km²，§13.71），而 L1/干线的点位要等走廊 OSM 重提取 + 吸附后才能进库，**届时必须重跑本表**（§1.12 落地顺序第④步）
- 服务时长固定 serviceSeconds（取 §0.2 实测装货 210 s + 卸货 264 s 之和 474 s），不含排队与人工干预
- 能耗 busyDrainMetersPerPercent = **1,800 m/1% SOC**（= 真车新石器 L4 满载 ≈180 km ÷ 100%，本人于 2026-09-23 提供的规格，与 FleetEnergyProperties 默认值一致）。**旧默认 150 m/1%（满电 15 km）低 12 倍，是 §13.72 撤掉那批产能/桩数结论的根因**；180 km 本身是"满载"保守值（标称 200 km ⇒ 2,000 m/1%），没扣温度与载重折损，所以它是**规格折算不是标定**，扰动带见 energy-sensitivity 那张扫描表
- 充电时长 chargeSeconds = **7,200 s / 次**（真车一次约 2 h，本人提供）。字段语义是"补满一个 SOC 工作带（20→90，70 个点）"的时长，把整笔 2 h 摊给这 70 个点 —— 这是 §1.1-b 的保守折算口径，比"按 0→100 比例折算（5,040 s）"悲观 30%。真车充电曲线无实测数据（§0.2），**假设，非标定**；库里 ZJF-CHG-01 的 avg_service_seconds 仍是仿真时代的 1,800 s，两套口径并存的收口挂在 §10.2 第 3 问
- 充电线性（默认）：一次补能的时长 = chargeSeconds ×（chargeCompleteSoc 90 − 当前 SOC）/（90 − 20），充到 90% 即恢复派单（三个阈值与 FleetEnergyProperties 同值）。回桩位移**默认不计**（chargeLayout 为空 = 原地瞬间开充，历史基线口径）；给了布局就按路网单程计入空驶与耗电，车落点移到该桩位所在点
- 充电曲线形状是**情景参数不是拟合**（Config.chargeCurve）：拐点与倍率都没有真车数据支撑（§0.2：仓库里没有真车真充电记录），所以只用它做敏感性扫描并给扰动带，不得称"充电模型"。默认 LINEAR 与历史逐字相同
- 补能点位只有 5 个站址是实测坐标（t_station GCJ 换米）。**库里 6 根桩 CP1..CP6 全挂在 ZJF-CHG-01 上且六个车位坐标逐字相同**，所以"选近桩"在现役设施下无从谈起 —— spreadSixOverFiveStations 那一臂是 §1.8 的情景假设（把同样 6 根桩摊到 5 个站址），不是现状；桩位在各点间的 2/1/1/1/1 分配同样是假设
- 补能时机用三档策略枚举（Config.chargeTiming），语义逐条对齐生产：NEVER = 只在必充阈值以下回桩；OPPORTUNISTIC = 现状 idleChargeWhenNoDemand=true（本 tick 无落空单就顺势补、只抢空桩不排队）；DEFER_UNDER_PRESSURE = 错峰推迟（近 pressureWindowTicks 个 tick 累计落单数达到 pressureDeferOrders 时连顺势补也推迟，但必充档不推迟 —— 同 shouldDeferReturnToCharge 的安全兜底）。注意：真实链路的高峰信号是 t_energy_forecast.pressure_p95 而不是 peak mode（§7.2 实测 peak cron 为 NULL）
- 不做路口冲突/MAPF：本实验台比较的是选车策略的相对差异，不是通行能力
- 事后基线只是**下界**：对每一笔被接下的单，取全车队（含繁忙车、不看 SOC 与占用）最近车辆的直线接驾距离 + 同样的载货段。它比实际里程必然偏小，所以 regret 偏乐观，不能当"最优解"引用
- 车辆位置在派单瞬间即改为卸货点。候选车本来就只从空闲车里选，所以这不影响选车距离，只会让事后下界更乐观（已并入上一条）
- §5 第 3 条列的"SOC 抛锚次数"在本模型里**恒为 0，且是构造性的**：全链路 SOC 前置检查（沿用生产的 canCompleteTaskWithSoc 语义）不允许接一趟跑不完的单，所以"跑到一半没电"不可观测。可观测的替代信号是 LOW_SOC 落单次数与"补能被桩位挡住"的车·分钟，两者都已导出
- 所有数字都必须带档位标签（S/M/L）；跨档引用即为违规（§1.3）
