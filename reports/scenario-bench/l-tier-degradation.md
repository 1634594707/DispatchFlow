# L 档退化曲线（12 次重复 · 95% t-CI）

**这不是 §1.3 的 L 目标规模档**：范围仍是现役 1613×500 m、桩仍是 6 根，只有车队与需求在动。
所以本表读作"运力的边际收益 / 规模放大时的不变性"，不得当作 40 台 @1.9 km²/10 桩的容量证明
（§1.3 档位不可混用；§9 禁止外推到 500 台）。

## A 轴：需求固定在 56 单每小时，只加车 —— 看每一台车的边际收益

| 配置 | 完成率 [95% CI] | 完成时长均值 | 总里程 | 补能被挡(车·分) | 空驶率 | 候选/次派单 |
| --- | --- | --- | --- | --- | --- | --- |
| A 需求固定 56/h · 10 台 | 0.3885 [0.3655, 0.4115] | 799.1 s | 69.2 km | 20.9 | 0.46 | 0.7 |
| A 需求固定 56/h · 20 台 | 0.8941 [0.8618, 0.9264] | 750.7 s | 136.9 km | 380.9 | 0.37 | 4.3 |
| A 需求固定 56/h · 40 台 | 1.0000 [1.0000, 1.0000] | 687.5 s | 120.3 km | 2394.6 | 0.18 | 24.0 |

## B 轴：需求随车数同比放大（保持运力/需求密度不变）—— 看规模本身会不会自带退化

| 配置 | 完成率 [95% CI] | 完成时长均值 | 总里程 | 补能被挡(车·分) | 空驶率 | 候选/次派单 |
| --- | --- | --- | --- | --- | --- | --- |
| B 需求随车数同比 · 10 台 / 28 单每小时 | 0.6556 [0.6089, 0.7023] | 779.1 s | 55.9 km | 32.9 | 0.43 | 1.5 |
| B 需求随车数同比 · 20 台 / 56 单每小时 | 0.8941 [0.8618, 0.9264] | 750.7 s | 136.9 km | 380.9 | 0.37 | 4.3 |
| B 需求随车数同比 · 40 台 / 112 单每小时 | 0.9903 [0.9824, 0.9981] | 714.9 s | 277.9 km | 1237.9 | 0.27 | 12.5 |


## 怎么读（下面每个数都由上表算出，方向由测试断言钉住）

**A 轴：完成率在 40 台这一档饱和了** —— 均值 1.0000，95% CI [1.0000, 1.0000]（宽度 0.0000）。
加车的边际收益在衰减：10→20 台 +50.56 pp，20→40 台 +10.59 pp（后者是前者的 21%）。
也就是说 40 台买到的不是"更多单"而是"更多空闲车"：那一档每次派单平均扫到 24.0 台空闲车，
约占车队的 60%。所以 A 轴最后一列读作 abundance 而不是 algorithm，
拿它当"L 档决策成本"会高估（需求没跟着涨，车当然闲着）。

**B 轴才是规模读法**：需求同比放大时完成率不退化反而升（0.6556 → 0.8941 → 0.9903），
所以在现役范围和 6 根桩下，**规模本身不吃完成率**。但同一张表里两列在超线性恶化：

- 补能被挡：32.9 → 380.9 → 1237.9 车·分。车只有 4× 而阻塞涨了 38×。
- 候选/次派单：1.5 → 4.3 → 12.5，40 台是 20 台的 2.9×。

桩侧压力和决策成本都在**完成率看不见的位置**上涨。这就是 L 档真正先撑不住的地方，
也是 §1.4 那条"热路径 277 ms 必须按候选集重测"的依据：按 B 轴口径，
40 台时每次派单要扫的车约是 20 台的 2.9 倍。
生产侧已核实是同一个形状：候选集就是**全量在线空闲车**（`DispatchVehicleAssignServiceImpl:165`
的 `listAssignableVehicles()`，没有距离/半径预筛），而每台候选要跑两次路径规划
（`canCompleteTaskWithSoc` 的可达性 + `estimateRouteDistance`，:198-216）——
所以候选 ×2.9 就是热路径耗时 ×2.9，减候选（距离预筛 / 空间索引）是 L 档最直接的杠杆。

**等派那一列被去掉了，原因要记着**：批量窗口=1 时接不上的单直接判 NO_VEHICLE 丢弃、不入队
（已核实主循环：backlog 每 tick 必冲，冲不动就记失败），所以该列结构性恒为 0。
它测的不是"没有等待"，而是"等待没有被建模"。缺单的代价记在完成率里，
要看等派只能在窗口 >1 的配置下看（见 batch 报告）。

本表的候选/次派单只是规模代理，不是时延。时延要用 `scripts/dev/bench-dispatch-latency.sh`
在真实服务上测，而且 MAPF 修复后旧数字已不可比（占用口径变了），必须重测后再引用。
